name: dada2-gpu-dev
channels:
  - conda-forge
  - nvidia
dependencies:
  - python=3.12
  - numpy
  - pytest
  - pip
  - make
  - zlib
  - gcc_linux-64
  - gxx_linux-64
  # GPU build/test toolchain. If you only need CPU builds, these can be removed.
  - cuda-nvcc=13.0
  - cuda-cudart-dev=13.0

